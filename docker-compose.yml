version: '3.8'

services:
  portfolio:
    build:
      context: .
      dockerfile: Dockerfile
    image: shreyans-portfolio:latest
    container_name: shreyans_portfolio_app
    ports:
      - "8000:3000"
    restart: always
